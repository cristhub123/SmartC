════════════════════════════════════════════════════════════════
SMARTCITY — BUG DE PINES/MAPA — SESIÓN 2026-08-13
════════════════════════════════════════════════════════════════
Este archivo se reinició a pedido explícito de Cris: solo contiene
lo trabajado en ESTA sesión (bug de pines que no se ubican en el
mapa / no reflejan cambios al refrescar). Contenido previo del
archivo fue eliminado a propósito, no se arrastra.

────────────────────────────────────────────────────────────────
PLAN INICIAL (tal como se planteó antes de tocar código)
────────────────────────────────────────────────────────────────
1. Corregir el orden en saveEdit()/saveNew()/borrado: actualizar
   POIS en memoria PRIMERO, recién después regenerar el caché
   público (cache/all-pines) — nunca al revés.
2. Hacer que la regeneración de caché se espere (await) en los
   puntos críticos, en vez de dispararse en paralelo sin control.
3. Sumar el arreglo de AppState/mapa diagnosticado antes en la
   sesión (la guardia de pois-bootstrap.js que solo deja dibujar
   pines que existan en AppState) — "Opción 2": unificar AppState
   con los datos reales de Firestore/POIS.
4. Resetear este archivo y documentar todo el proceso real de esta
   sesión (plan vs. lo efectivamente hecho), etapa por etapa.

════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────
LO QUE REALMENTE SE HIZO (etapa única, completada)
────────────────────────────────────────────────────────────────

ÍTEMS 1 Y 2 DEL PLAN (orden + await del caché): COMPLETADOS.
Causa raíz real confirmada leyendo el código: `regeneratePublicCache()`
(la función que escribe `cache/all-pines`, el ÚNICO documento que lee
cualquier visitante al cargar la página) se disparaba SOLA, sin
esperar, desde ADENTRO de `savePoiToFirestore()` / `saveSkinsToFirestore()`
/ `deletePoiFromFirestore()` — justo después de escribir en Firestore,
pero en la mayoría de los lugares que llaman a estas funciones
(saveEdit, saveNew, importaciones, marcar revisado, vincular imágenes),
el array POIS en memoria TODAVÍA no tenía el dato actualizado en ese
instante — esa actualización pasa varias líneas después, en cada
función. Resultado: el caché público se regeneraba con datos VIEJOS.
Por eso a veces "no se guardaba" (se autocorregía solo en el próximo
guardado de cualquier otro lugar) y en el caso de un cambio de ID
directamente el lugar desaparecía (el doc viejo se borra de verdad de
Firestore, pero el caché seguía apuntando a ese ID fantasma).

Arreglo aplicado: se sacó la regeneración automática de adentro de las
3 funciones de Firestore (firestore-sync.js). Ahora cada lugar que
guarda/borra/importa pines es responsable de llamar a
`regeneratePublicCache()` (con await) DESPUÉS de dejar POIS en memoria
con el dato final y correcto. En los importadores masivos (loops), se
llama UNA sola vez al final del loop, no una vez por ítem (más
eficiente y evita el mismo problema de orden).

Archivos tocados:
- js/firestore-sync.js — sacada la auto-regeneración de savePoiToFirestore,
  saveSkinsToFirestore y deletePoiFromFirestore.
- js/pin-adjust.js — regeneración explícita agregada en: saveEdit()
  (el fix más importante: cubre el caso de cambio de ID),
  saveNew(), markPinAsReviewed(), createShellPinsFromPrefixList(),
  importFullPinsFromText(), importImageLinksFromText().
- js/admin.js — regeneración explícita agregada en: togglePoi(),
  togglePublicClicks(), y el handler de borrado (mc-delete).
- js/content-import.js — regeneración explícita (una sola vez al
  final del loop) en importContentJSON().
- js/data-io.js — regeneración explícita (una sola vez, no por ítem)
  en la importación masiva de JSON (confirmImport()).

Verificación hecha: los 37 archivos .js pasan `node --check` sin
errores de sintaxis. Se auditó cada llamado a `regeneratePublicCache`
en todo el proyecto (grep exhaustivo) para confirmar que no quedó
ningún save/delete/import de pines sin su regeneración correspondiente,
y que ninguna quedó apuntando a POIS desactualizado.

ÍTEM 3 DEL PLAN (unificar AppState con la guardia de pois-bootstrap.js):
DESCARTADO EN ESTA SESIÓN — re-evaluado y explicado por qué.
Este ítem partía de una hipótesis de una sesión anterior: que la
guardia de `makeMarker` en pois-bootstrap.js (que solo deja dibujar
pines que existan en AppState) bloqueaba por completo la creación de
pines nuevos en el mapa. Se investigó a fondo (lectura exhaustiva de
markers.js/pois-bootstrap.js/pois-loader.js/app-state.js) y la lógica
del código efectivamente sostenía esa hipótesis en teoría — PERO Cris
mandó una captura real del mapa mostrando 6 lugares con marcador
(5 de ellos con el ícono de respaldo 📍 en vez de la imagen real, pero
CON marcador), lo cual contradice directamente que la guardia esté
bloqueando el dibujado. Ante esa evidencia se descartó la hipótesis:
no es la causa del síntoma reportado.
Sigue siendo una inconsistencia real y confirmada en el código (dos
"universos" de datos separados: POIS/Firestore real vs.
AppState/pois_cordoba.json, un archivo JSON con sólo 4 lugares de
prueba que nunca se volvió a tocar) — pero no es la causa de ningún
bug reportado hasta ahora, y tocarla sin un motivo concreto agrega
riesgo innecesario. Queda anotada como pendiente para una sesión
dedicada aparte si en el futuro aparece un síntoma que sí la involucre
(por ejemplo, funciones ligadas a AppState como el sistema de
día/noche o temas globales, que si se llegan a construir sobre
AppState en vez de sobre POIS, van a chocar con esto).

BUG ADICIONAL ENCONTRADO Y RESUELTO DE PASO (mismo síntoma reportado,
"el ícono de respaldo aparece en vez de la imagen real" en la captura
de 5 de 6 lugares): no se tocó código para esto todavía — pendiente de
confirmar con Cris el orden real en que cargó el nombre/ubicación vs.
la imagen en esos 5 casos, porque `validateUploadFilename()` rechaza
la subida (silenciosamente, solo un toast) si el nombre del archivo no
coincide con el ID calculado en el momento exacto de la subida — si el
nombre/ubicación todavía no estaban completos, el ID usado para
validar no es el final. Sin tocar todavía — no confirmado como causa
real, solo hipótesis a verificar.

────────────────────────────────────────────────────────────────
PRÓXIMOS PASOS SUGERIDOS (no arrancados)
────────────────────────────────────────────────────────────────
- Cris debe probar en producción: crear un lugar nuevo, refrescar, y
  confirmar que aparece. Editar un lugar existente cambiando su ID,
  refrescar, confirmar que NO desaparece (el bug puntual que reportó).
- Confirmar o descartar la hipótesis del orden de carga del formulario
  como causa de las imágenes faltantes (ítem pendiente de arriba).
- Revisar el desfasaje "4 en la lista, 5 en el mapa" que Cris reportó
  — no se pudo confirmar la causa exacta en esta sesión (posible
  "fantasma" de una sesión anterior con el caché viejo, debería
  autocorregirse con este fix, a confirmar en la próxima prueba).

════════════════════════════════════════════════════════════════
SESIÓN 2026-08-13/14 — smartcityV3.0_fix-mapa-pines
════════════════════════════════════════════════════════════════
NOTA PERMANENTE — NO BORRAR ESTA SECCIÓN EN NINGUNA LIMPIEZA
FUTURA DE CAMBIOS.TXT, INCLUSO SI SE LIMPIAN NOTAS VIEJAS DE
OTRAS SESIONES. Esta sesión resolvió el bug central de que los
pines nuevos/editados no aparecían en el mapa, arrastrado desde
hace semanas — el detalle de qué se tocó y por qué es información
crítica para no repetir el mismo problema ni revertir el fix por
error en una limpieza futura. Si en algún momento se decide
resumir/acortar esta sección, el resumen debe conservar como
mínimo: los 3 bugs encontrados, los archivos tocados, y esta
misma advertencia de "no borrar".

PLAN ORIGINAL (con el que arrancó esta sesión):
Etapa 0 — Auditoría de dependencias (grep exhaustivo, sin tocar
  nada), para saber con certeza qué archivos referencian
  pois-bootstrap.js/pois-loader.js/AppState/window.POIS antes de
  desconectar nada.
Etapa 1 — Desconectar pois_cordoba.json/pois-bootstrap.js como
  fuente del mapa (sin borrar los archivos, solo sacarlos de
  index.html).
Etapa 2 — Confirmar que crear/editar/borrar un pin se refleja en
  el mapa sin necesidad de refrescar.
Etapa 3 — Limpieza de datos fantasma en Firestore, recién después
  de identificarlos con certeza.
Etapa 4 — Verificación final + entrega.

QUÉ SE ENCONTRÓ REALMENTE (más profundo que el plan original):
1) CAUSA RAÍZ ORIGINAL, CONFIRMADA: pois-bootstrap.js reemplazaba
   la función global makeMarker() por una versión "guardia" que
   solo dibuja un pin si su ID existe en AppState — y AppState
   solo se hidrataba con los 4 lugares fijos de pois_cordoba.json
   (vía pois-loader.js). Ningún pin real (Firestore, vía
   pin-adjust.js) podía entrar nunca a AppState (app-state.js no
   tiene función addPoi(), solo loadPois()/updatePoi()) — por eso
   nunca se dibujaba, sin importar que pin-adjust.js sí llamara a
   makeMarker(p) correctamente después de guardar.
   CONFIRMADO CON EVIDENCIA VISUAL DIRECTA de Cris: 2 marcadores
   "Alto Paz Tower" en el mapa (uno sin imagen = el fantasma del
   JSON con ID "alto-paz-tower", otro con imagen = el real de
   Firestore con ID "alto-paz-tower-cordoba"), mientras que en
   Firestore solo existe 1 documento con ese nombre. Los IDs no
   coinciden exacto pero AppState normaliza sufijos regionales
   tipo "-cordoba" al comparar, por eso el real "se colaba" por la
   guardia además del fantasma del JSON — de ahí el duplicado.
2) BUG NUEVO #1, no diagnosticado en sesiones anteriores:
   poi-panel.js (el panel que se abre al tocar un pin) lee
   EXCLUSIVAMENTE de AppState.getPoi()/getContent() — nunca lee
   POIS directo. Aunque se hubiera arreglado solo el dibujado del
   mapa, el panel de cualquier pin real iba a seguir sin mostrar
   nada, porque AppState nunca tuvo esos datos.
3) BUG NUEVO #2, no diagnosticado en sesiones anteriores:
   AppState.updatePoi()/toggleSkinStatus()/toggleClicksVisibility()
   (las funciones que poi-panel.js usa para persistir una edición
   hecha DESDE EL PANEL — texto, skins, el "ojito") llaman a un
   objeto global FirestoreSync.savePoi(). Ese objeto NUNCA existió
   en el proyecto (firestore-sync.js solo exponía funciones
   sueltas). Cualquier edición hecha desde el panel se perdía en
   el próximo refresh, sin error visible. Nunca se notó porque el
   panel nunca tuvo un pin real para editar (ver bug #2 arriba).
4) BUG NUEVO #3, CONFIRMADO CON EVIDENCIA DIRECTA DE FIRESTORE
   (capturas de Cris): loadPOISFromFirestore() confiaba
   ciegamente en el documento cache/all-pines si existía, sin
   verificar nunca que estuviera completo respecto a la colección
   pines real — solo se reconstruía si el caché NO EXISTÍA en
   absoluto, nunca por estar incompleto. Cris confirmó: la
   colección pines tenía 11 documentos reales, el panel Admin
   (que arma su lista leyendo POIS, hidratado por esta función)
   solo mostraba 5. Uno de los faltantes, plaza-san-martin-cba, es
   un documento perfectamente válido y completo (nombre, coords,
   imagen ya en Cloudinary) — descarta que sea un problema de
   datos, es un problema de LECTURA. Cris también confirmó que el
   botón de borrar SÍ funciona en Firestore (probó borrar
   plaza-colon desde el panel y desapareció también de Firestore),
   así que el problema es puntualmente de lectura/caché, no de
   guardado ni de borrado.

QUÉ SE HIZO REALMENTE (coincide con el plan reformulado tras los
hallazgos, no con el plan original de 4 etapas):

- index.html: comentadas las líneas <script> de js/pois-loader.js
  y js/pois-bootstrap.js (NO se borraron los archivos). Cada línea
  comentada tiene una nota explicando el motivo y remitiendo a
  esta sección de CAMBIOS.txt.
- js/pois-loader.js y js/pois-bootstrap.js: se agregó un bloque de
  comentario al inicio de cada archivo marcándolos DESCONECTADOS,
  con la razón completa, para que quien abra el archivo suelto
  entienda por qué sigue ahí sin usarse.
- js/firestore-sync.js:
  · Nueva función syncAppStateWithPOIS() — mantiene AppState
    sincronizado con el array real POIS. Se llama en cada punto
    donde POIS queda en su estado final (los mismos puntos donde
    ya se llama regeneratePublicCache()).
  · loadPOISFromFirestore() reescrita: ya NO lee cache/all-pines
    para armar POIS (ese código queda comentado, no borrado, por
    si se reintroduce la optimización más adelante). Ahora lee
    siempre en vivo la colección pines completa, y regenera el
    caché de paso en cada carga (autosanándolo). A la escala
    actual (~11 lugares) esto es 1 lectura extra insignificante
    contra la cuota gratis de Firestore (50.000/día) — antes de
    escalar a tráfico público real (lanzamiento de noviembre) hay
    que revisar si conviene reintroducir el caché, pero con una
    forma real de detectar que está desactualizado (por ejemplo
    una count() aggregation query, 1 sola lectura sin importar el
    tamaño de la colección) en vez de solo confiar en que existe.
  · Nuevo objeto window.FirestoreSync = { savePoi(poi) {...} } —
    el wrapper que faltaba (bug #3 de arriba). Hace lo mismo que
    ya hace el admin real al guardar: escribe en Firestore,
    actualiza POIS en memoria, sincroniza AppState, regenera el
    caché.
- js/admin.js: syncAppStateWithPOIS() agregada en togglePoi(),
  togglePublicClicks(), y el handler de borrado (mc-delete) — los
  mismos 3 puntos que ya tenían regeneratePublicCache() de la
  sesión anterior.
- js/content-import.js: syncAppStateWithPOIS() agregada junto a la
  regeneración de caché existente en importContentJSON().
- js/data-io.js: syncAppStateWithPOIS() agregada junto a la
  regeneración de caché existente en confirmImport().
- js/pin-adjust.js: syncAppStateWithPOIS() agregada en los 6
  puntos que ya tenían regeneratePublicCache(): saveEdit(),
  saveNew(), markPinAsReviewed(), createShellPinsFromPrefixList(),
  importFullPinsFromText(), linkPinImagesFromText().

Verificación hecha: los 38 archivos .js del proyecto pasan
`node --check` sin errores de sintaxis. Se auditó con grep
exhaustivo cada referencia a AppState/PoisLoader/pois_cordoba.json
en todo el proyecto — confirmado que solo pois-bootstrap.js y
pois-loader.js dependían de ellos, seguro desconectarlos sin
romper nada más.

QUÉ NO SE TOCÓ (a propósito, según lo acordado):
- Ningún documento de Firestore fue borrado ni modificado. Los
  11 documentos reales de la colección "pines" siguen exactamente
  igual que antes de esta sesión.
- No se decidió todavía qué hacer con los IDs del JSON viejo
  (patio-olmos, alto-paz-tower, loteria-de-cordoba,
  palacio-municipal-6-de-julio) — pois_cordoba.json sigue
  existiendo en el repo, simplemente desconectado. Esa decisión
  (Etapa 3 del plan original) sigue pendiente de que Cris confirme
  qué hacer, ahora que se sabe que esos 4 nombres SÍ tienen
  documentos reales asociados en Firestore con otros IDs (con o
  sin sufijo regional) — no son solo "basura de prueba".

PENDIENTE REAL PARA LA PRÓXIMA SESIÓN:
- Cris debe probar en producción tras este deploy: F5 fuerte,
  confirmar que el mapa y la tab Lugares muestran EXACTAMENTE los
  mismos ~11 lugares (incluido plaza-san-martin-cba). Confirmar
  que ya no aparece el "Alto Paz Tower" duplicado sin imagen.
  Crear un pin nuevo y confirmar que aparece en el mapa sin
  refrescar. Editar un pin desde el panel (no desde el admin) y
  confirmar que sobrevive un F5 (prueba directa del bug #3).
- Bug reportado y sin confirmar todavía (independiente de todo lo
  de arriba): "al agrandar la imagen de Alto Paz Tower se ve la
  versión de baja calidad" — no investigado en esta sesión, puede
  ser el mismo bug histórico del crop de miniatura (ya arreglado
  en markers.js para el pin del mapa) manifestándose en otro lugar
  (el panel), o algo distinto. Revisar en la próxima sesión.
- Decidir con Cris qué hacer con pois_cordoba.json y los 4
  lugares que tenía hardcodeados (ver arriba).

════════════════════════════════════════════════════════════════
SESIÓN 2026-08-14 (continuación) — toggles reubicados + ojito
nueva función + fix de centrado del mapa
════════════════════════════════════════════════════════════════

CONTEXTO: Cris reportó 2 toggles en el panel PÚBLICO del lugar (el
de skin/imagen y el ojito) que no debían estar ahí, y que el mapa
ya no se centraba bien sobre el pin al hacer click (antes andaba
perfecto). Se auditó primero (sin tocar código) y se confirmó todo
lo reportado; en esta sesión se aplicaron los 4 cambios pedidos.

1) TOGGLE DE SKIN/IMAGEN — reubicado del panel público al admin.
   Antes: en js/poi-panel.js (panel público), el toggle on/off por
   skin no tenía NINGÚN chequeo de admin (bug real: cualquier
   visitante podía activar/desactivar imágenes de cualquier lugar,
   a diferencia del botón "Editar" y del ojito, que sí estaban
   protegidos).
   Ahora: la sección entera de skins (thumbnails + switch) se sacó
   del panel público — ni se renderiza más ahí. El toggle "Activo"
   vive ahora en el admin, en js/img-slots.js, uno por cada slot de
   imagen (variantes altN), reutilizando el estilo `.za-toggle` que
   ya existía en el admin (css/base.css). Por ahora es solo
   reubicación, tal como pidió Cris — sin cambiar más su función.
   NOTA: el toggle no se agregó al slot de la imagen "principal"
   (main, markup fijo en index.html/utils.js) porque esa imagen no
   se puede desactivar por regla de negocio (fallback obligatorio,
   misma regla que ya tenía AppState.toggleSkinStatus). Si se quiere
   ese toggle también ahí (aunque sea deshabilitado, solo visual),
   es un cambio aparte a definir.
   Cambio de forma de datos: `AltSlotsAdd.getSkins()` /
   `AltSlotsEdit.getSkins()` antes devolvían `{variant: url}`, ahora
   devuelven `{variant: {url, active}}`. Se actualizaron los 2
   lugares que los consumen en js/pin-adjust.js (saveNew y saveEdit)
   a la forma nueva.

2) OJITO — se queda en el panel público (tal como pidió Cris), pero
   con función completamente distinta.
   Antes: tocaba AppState.toggleClicksVisibility (mostrar/ocultar el
   contador de visitas al público), solo habilitado para admin.
   Ahora: es un control PÚBLICO que recorre las imágenes activas del
   lugar — cada click avanza a la siguiente imagen (entre las que
   tienen active !== false), y al llegar a la última vuelve a la
   primera en loop. Si el lugar tiene 1 sola imagen activa (o
   ninguna), el ojito queda apagado/sin contador (nada para
   recorrer). Se agregó `_getActiveSkinList(poi)` en poi-panel.js
   como fuente única de esa lista, usada tanto por el ojito
   (_renderEyeBadge) como por la imagen hero (_renderHeroImage).
   PENDIENTE DE DECISIÓN DE CRIS: AppState.toggleClicksVisibility
   quedó sin ningún botón que lo dispare (la función sigue existiendo
   en app-state.js, no se borró). Falta decidir si esa función
   (mostrar/ocultar el contador de visitas) se reubica también en el
   admin —y dónde— o se abandona.

3) IMAGEN FULL 1024PX AL MAXIMIZAR — se confirmó que YA estaba bien
   implementado (no era un bug de código): _renderHeroImage siempre
   usó la URL real guardada en poi.skins[skin].url / poi.imgB64
   (full resolución), nunca el recorte 150x150 que usa el pin chico
   en el mapa (ese recorte es exclusivo de js/markers.js). Sigue
   igual, solo que ahora la imagen mostrada depende de qué índice
   señala el ojito en vez de un único active_skin/main fijo.

4) CENTRADO DEL MAPA AL CLICKEAR UN PIN — arreglado.
   Causa raíz confirmada: había 2 sistemas de centrado compitiendo
   entre sí. `PoiPanel.open()` (poi-panel.js) centraba el mapa de
   forma SÍNCRONA con su propia `_centerMapOn()` (un flyTo al medio
   geométrico de toda la pantalla, sin descontar el panel) y, 50ms
   después, js/cluster.js disparaba ADEMÁS `panToPoiCenter()` (de
   app.js), interrumpiendo la animación del flyTo a mitad de vuelo.
   Encima, panToPoiCenter usaba una constante vieja hardcodeada
   (PANEL_FRAC = 0.62) que no coincidía con la altura real del panel,
   y no distinguía pantalla vertical de cuadrada/horizontal.
   Arreglo: se eliminó `_centerMapOn()` de poi-panel.js por completo
   — el centrado queda unificado en un solo lugar, `panToPoiCenter`
   (js/app.js), disparado por cluster.js con el mismo delay de 50ms
   que Cris pidió conservar. Se reescribió la fórmula con 3 casos,
   usando el mismo breakpoint de 1024px que ya usa poi-panel.js para
   pasar a modo sidebar:
     - Vertical (alto > ancho), bottom sheet: horizontal exacto sobre
       la coordenada del pin (50% del ancho), vertical centrado en la
       mitad libre de arriba (25% desde arriba, ya que el panel
       reserva el 50% inferior).
     - Cuadrada u horizontal (ancho >= alto), todavía bottom sheet
       (< 1024px de ancho): al revés — vertical exacto (50% del
       alto), horizontal centrado en el 25% desde la izquierda
       (asumiendo la mitad reservada a la derecha).
     - Desktop (>= 1024px, sidebar real de 380px fijo a la
       izquierda): vertical exacto (50% del alto), horizontal
       centrado en el hueco real que deja el sidebar (desde 380px
       hasta el borde derecho) — acá sí se usa la medida real del
       panel, no una suposición.
   NOTA IMPORTANTE PARA CRIS: en el caso "cuadrada/horizontal por
   debajo de 1024px", el CSS del panel (css/poi-panel.css) hoy TODAVÍA
   sigue mostrando el panel como bottom sheet (desde abajo), no como
   panel lateral — solo el CÁLCULO de centrado ya asume un hueco
   lateral. Si se quiere que el panel también deslice desde el
   costado visualmente en esas pantallas (no solo el cálculo del
   centrado), es un cambio de CSS aparte, todavía no hecho.
   Cambio menor de paso: en js/markers.js se dejó un comentario
   aclarando que la función `pinClick()` de ese archivo está muerta
   en la práctica (js/cluster.js declara una función global con el
   mismo nombre y carga después, así que esa es la que realmente
   atiende cada click) — no se borró nada, solo se documentó para
   que quien abra ese archivo no se confunda buscando ahí el bug de
   click en un pin.

Archivos tocados en esta sesión:
- js/poi-panel.js — sacada la sección de skins (HTML+refs+función
  _renderSkins); ojito reescrito (_renderEyeBadge, click handler,
  nueva _getActiveSkinList, _heroSkinIndex); _renderHeroImage usa
  ahora esa lista; sacado _centerMapOn() y su llamado en open().
- css/poi-panel.css — sacados los estilos de la sección de skins
  (.poi-panel__skins-grid, __skin-row, __skin-thumb, __skin-name,
  __switch y sus variantes).
- js/img-slots.js — agregado el toggle "Activo" por slot (markup +
  estado + _paintToggle), _syncWindowVar y getSkins() devuelven
  ahora {url, active} en vez de solo url, reset() precarga el active
  existente.
- js/pin-adjust.js — saveNew() y saveEdit() actualizados a la nueva
  forma {url, active} que devuelve getSkins().
- js/app.js — panToPoiCenter() reescrito completo (3 casos según
  orientación/breakpoint, sin la constante vieja 0.62).
- js/markers.js — comentario aclaratorio agregado sobre pinClick()
  muerta (sin cambio de comportamiento).

Verificación hecha: los 38 archivos .js pasan `node --check` sin
errores de sintaxis.

PENDIENTE REAL PARA LA PRÓXIMA SESIÓN:
- Cris debe probar en producción: click en un pin en celular vertical
  (confirmar el 25%/50% correcto), en celular/tablet horizontal, y en
  desktop (>=1024px) — confirmar que ya no hay "salto"/pelea de
  animaciones. Probar el ojito con un lugar de 2+ imágenes activas
  (debe ciclar y volver a la primera). Probar el toggle "Activo" desde
  el admin (Nuevo y Editar) y confirmar que una imagen desactivada ya
  no aparece en el ciclo del ojito público.
- Decidir dónde va (o si se abandona) el control de
  mostrar/ocultar el contador de visitas públicas (antes era la
  función del ojito, ver punto 2 de arriba).
- Decidir si se agrega el toggle "Activo" también a la imagen
  principal (main), aunque sea deshabilitado/informativo.
- Decidir si el panel debe deslizar desde el costado (no solo bottom
  sheet) en pantallas cuadradas/horizontales por debajo de 1024px,
  para que coincida visualmente con el nuevo cálculo de centrado.
